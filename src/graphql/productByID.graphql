query productByID($id: String!) {
    product(id: $id) {
        id,
        name,
        description,
        price,
        reviews {
            id,
            text,
            rating,
            userId,
            productId,
            createdAt,
            updatedAt,
        }
    }
}